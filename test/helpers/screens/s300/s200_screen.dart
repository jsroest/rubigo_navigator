import 'package:flutter/widgets.dart';

class S300Screen extends StatelessWidget {
  const S300Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
