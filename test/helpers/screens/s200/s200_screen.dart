import 'package:flutter/widgets.dart';

class S200Screen extends StatelessWidget {
  const S200Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
